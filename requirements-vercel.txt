# Slim dependency set for the Vercel API service (Vercel Python runtime).
# The backend serves SIMULATED results from SQLite; the heavy ML stack
# (torch / shap / weasyprint) is only needed offline by scripts, not by
# the API at request time. scipy + scikit-learn are installed by the
# service buildCommand (they power seed_database.py) and are therefore not
# listed here.
fastapi>=0.115
sqlalchemy>=2.0
pydantic>=2.8
httpx>=0.27
numpy>=2.0
pandas>=2.2